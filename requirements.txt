fastapi>=0.115.0
uvicorn[standard]>=0.30.0
celery>=5.4.0
redis>=5.0.8
sqlmodel>=0.0.21
psycopg2-binary>=2.9.9  # needed only if DATABASE_URL points at Postgres (docker-compose default)
python-dotenv>=1.0.1
pillow>=10.4.0
numpy>=1.26.4
requests>=2.32.3
edge-tts>=6.1.9       # Free TTS: Microsoft neural voices (Urdu: ur-PK-UzmaNeural)
gTTS>=2.5.1           # Free TTS fallback: Google Translate endpoint
pytest>=8.3.2

# Real-service integrations live in requirements-full.txt (they're heavy:
# torch/transformers alone is several GB). Install that file only once
# you're ready to flip on Sentinel Hub / Qwen-VL / Google Cloud / Twilio.
